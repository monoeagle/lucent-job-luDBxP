# Session Handoff — 2026-06-27 00:28

## Was wir gemacht haben
**LucentTools DB Explorer** (lucent-job-luDBxP) — Flask-Tool für Join-Pfade über
DB-Schemas (FK-Graph → read-only SQL). Diese (lange) Linux-Session hat fünf APs
abgeschlossen (**v0.12.0 → v0.16.0**), den Backlog konsolidiert und Doku + GitHub
Pages durchgehend aktuell gehalten. Alles nach `origin/master` gepusht; die
Doku-Site ist live unter https://monoeagle.github.io/lucent-job-luDBxP/.

## Erledigt
- [x] **AP-15** (v0.12.0) — `run.sh` abbruchsicher + idempotent (Parität zu run.ps1); adaptiver NO-CDN-Install; Bug gefixt (leeres venv galt als vollständig → auch in run.ps1 = **AP-35**)
- [x] **AP-33** (v0.13.0) — Logging: Rotation, Level/Pfad via ENV (`LUCENT_LOG_*`), Request-Logging in `web/`; Pro-Nutzer-Logpfad-Hook für AP-31
- [x] **AP-14** (v0.14.0) — Python-3.14-AppImage (uv user-lokal, cp314-manylinux); AppRun-Fixes: versions-bewusstes Update + Chrome-statt-Firefox; run.sh maxsplit-Fix
- [x] **AP-29** (v0.15.0) — SQL-Dialekt umschalten (hand-gerollte `Dialect`-Schicht in `core/sqlgen.py`): Quoting + LIMIT/TOP/FETCH je Dialekt; Anzeige≠Ausführung. **Identifier werden jetzt immer quotiert.**
- [x] **AP-12** (v0.16.0, komplett) — MSSQL: System-ODBC (msodbcsql18), skip-guarded Integrationstest **real grün** gegen SQL Server 2022, UI-Felder Encrypt/Trust (Tri-State, persistiert)
- [x] **Backlog konsolidiert:** AP-17 gestrichen (Releases), AP-22 (impl. FKs OFF) + AP-24 (KPIs dev-intern) entschieden, AP-30 als N-1-Stern zugeschnitten
- [x] Doku-Drift nachgezogen (Tests 81→138, Python 3.12→3.14, run.sh-Menü, Fixtures), **neue UI-Screenshots**, AP-Baum/Gantt/Roadmap je AP nachgezogen + Pages deployed
- [x] `onboarding.html` (global, 39 Projekte/89 Sessions ausgewertet) unter `~/Dokumente/_Projects/` abgelegt

## In Arbeit / Nächste Schritte
- [ ] **SOFORT — Entscheidung Onboarding-Dateien:** `ONBOARDING.md`, `onboarding.html`, `onboarding-compact.html` (Repo-Root) sind **untracked** — committen oder verwerfen? (aus `/team-onboarding`)
- [ ] **Nächstes AP (Empfehlung AP-30):** N-1-Stern-Abfrage — Start zieht Attribute aus mehreren Lookup-/Eltern-Tabellen in EINEM SELECT (kein Fan-out), 1-N wird gewarnt. Betroffen: `core/pathfinder.py`, `core/sqlgen.py`, `web/static/js/app.js`. Design steht in `todo.md` AP-30.
- [ ] Alternativ: **AP-31** Terminal-Server (dynamischer Port + Pro-Nutzer-Pfade; Logpfad-Hook aus AP-33 schon da) · **AP-25** SQL-Analyzer (groß, sqlglot) · **AP-19** .pattern_transfer
- [ ] **MSSQL-Container aufräumen:** `podman rm -f lucent-mssql` wenn nicht mehr gebraucht (läuft noch, ~1–2 GB RAM)
- [ ] **KPI-Lücke:** Session 4 (Windows) fehlt weiter in `docs/session-kennzahlen.md` (siehe AP-24)

## Entscheidungen dieser Session
- **AP-29 Dialekt:** hand-gerollt (keine sqlglot-Dependency — Unterschiede klein/bounded; sqlglot für AP-25 aufgehoben); **immer quotieren** (korrekt/reserved-word-sicher); Anzeige nutzt gewählten Dialekt, Ausführung den der echten Verbindung.
- **AP-12 MSSQL-Testinstanz:** **rootless podman** statt Docker (docker.io scheiterte am containerd.io-Konflikt mit dem MS-Repo).
- **AP-17 gestrichen** (GitHub-Releases ersetzen ein Delivery-Verzeichnis); **AP-22** impl. FKs bleiben **opt-in**; **AP-24** KPIs bleiben **dev-intern** (nicht publiziert).

## Umgebung (wichtig für die nächste Session)
- **Python 3.14.6** via `uv` user-lokal; `~/.local/bin/python3.14` ist auf PATH → `run.sh`/venv nutzen 3.14. venv ist 3.14 (online-installierte cp314-manylinux-Wheels).
- **MSSQL-Container** `lucent-mssql` (podman, SQL Server 2022, `sa`/`LucentPass123`, Port 1433) läuft. Integrationstest real:
  `LUCENT_MSSQL_TEST_URL='mssql+pyodbc://sa:LucentPass123@localhost:1433/master?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes' ./venv/bin/python -m pytest tests/test_mssql_integration.py`
- **Doku-Build nur Linux:** `cd luDBxP-docs && bash run_luDBxP_docs.sh --build`; Deploy = `site/`-Inhalt nach `gh-pages`-Branch (Worktree-Pattern), Pages-Source = gh-pages root.

## Git-Status
- Branch: `master`, **synchron mit `origin/master`** (`0606cec`), v0.16.0, **138 Tests grün, 1 skipped**.
- Uncommitted: nur **untracked** `ONBOARDING.md`, `onboarding.html`, `onboarding-compact.html`.
- `gh-pages` deployed (`9f418b8`). Stash: none.
- **Konvention:** Commits ohne KI-Signatur.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit **LucentTools DB Explorer** (lucent-job-luDBxP), Linux.
Letzte Session: 2026-06-27 00:28, Stand **v0.16.0**, 138 Tests grün, alles nach origin/master gepusht, Pages live.
**ZUERST:** `git fetch` + Divergenz prüfen; `./venv/bin/python -m pytest` als Baseline (venv ist Python 3.14).
Offene Entscheidung: 3 untracked Onboarding-Dateien im Repo-Root — committen oder verwerfen?
Nächstes AP-Vorschlag: **AP-30** (N-1-Stern-Abfrage) — Design in `todo.md`. Alternativen: AP-31/AP-25/AP-19.
Umgebung: Python 3.14 via uv (`~/.local/bin/python3.14`); MSSQL-Container `lucent-mssql` (podman, sa/LucentPass123:1433) läuft für AP-12-Tests — bei Nichtgebrauch `podman rm -f lucent-mssql`.
Konventionen: Deutsch · NO-CDN · Doku-Build nur Linux · je AP committen (ohne KI-Signatur) · 1.0.0 nur auf Ansage · bei ~70 % Kontext Handoff.
Handoff-Datei: docs/handoffs/2026-06-27-0028.md
