# Session Handoff — 2026-06-26 02:52

## Was wir gemacht haben
Lucent DB Explorer (visueller Join-Pfad-Builder, Flask) von v0.1.0 auf **v0.3.1**
erweitert: AP-1…AP-9 (Graph-Interaktion, SQL-Optionen, mehrere SELECT-Spalten,
Ausführen+Ergebnistabelle, Zeilen-Steuerung, feiner Graph-Zoom, Reset-Fix,
maximierte Ergebnisliste) plus Offline-Wheelhouse/Windows, Doku-Site-Feinschliff
(Startseite 2-spaltig, AP-Übersicht 3-spaltig, Versionsanzeige-Drift behoben) und
gh-pages-Deploys.

## Erledigt (diese Session)
- [x] AP-5 Ausgabebereich: generiertes SELECT **ausführen** → Ergebnistabelle (v0.2.0)
- [x] AP-6 Ausgabe-Steuerung: Zeilen 200/400/Alle + „Aktualisieren" (v0.3.0)
- [x] AP-7 Feiner Graph-Zoom (wheelSensitivity 0.2) + Zoom-Slider mit % (v0.3.0)
- [x] AP-8 Fix „Auswahl zurücksetzen": Pfad-Highlight + UML-Karten leeren (v0.3.0)
- [x] AP-9 Ergebnisliste maximiert (voller Platz nach unten, kein 320px-Cap) (v0.3.1)
- [x] Doku: AP-Diagramm (3-spaltig) + Gantt + Changelog + Roadmap auf v0.3.1
- [x] Oberflächen-Screenshots (7×, 1400×900) neu aufgenommen (mit Ergebnistabelle + Zoom-Slider)
- [x] Startseite: Architektur-Übersicht links + AP-Band 3-spaltig rechts, fit-by-height (kein Scrollen)
- [x] Versions-Drift behoben: icon-rail.js (APP_VERSION/TEST_COUNT/TEST_DATE), zensical.toml, make_poster.py → 0.3.1 / 111 Tests
- [x] Site neu gebaut + nach master **und** gh-pages deployed
- [x] 111 Tests grün; Playwright-Verifikation aller UI-Features
- [x] **Projektposter neu** (nach Handoff): Layout Architektur-Band oben + AP-Band (querformat) + Screenshots 4-spaltig, Gantt entfernt; mail/-Assets (Screenshots/Diagramme/Poster) auf v0.3.1. `make_poster.py` umgebaut (Diagramme über das Raster, COLS 3→4, Höhen-Aufteilung via `* 0.62`-Faktor)

## In Arbeit / Nächste Schritte
- [ ] **Keine offenen APs** — `todo.md` ist leer. Auf nächste Anforderung des Users warten.
- [ ] Optional/Backlog: bekannte v1-Einschränkungen (composite FKs nur erste Spalte; Postgres nur via SQLAlchemy, Tests nur SQLite) — nur falls gewünscht.
- [ ] Optional: `sync_version.py` so erweitern, dass es auch icon-rail.js/zensical.toml/make_poster.py mit-bumpt (heute manuell) — verhindert die Versions-Drift dauerhaft.

## Entscheidungen dieser Session
- **read-only aufgeweicht (bewusst):** Generiertes Join-SELECT wird ausgeführt, aber serverseitig gebaut, parametrisiert, Zeilen-Cap (`MAX_RESULT_ROWS`). Siehe Insight §4.
- **Per-AP-Versionierung:** Feature→`--minor`, Fix→`--patch` via `sync_version.py`; jede AP committen, bevor die nächste beginnt. AP-6/7/8 mussten gebündelt werden (geteilte Frontend-Dateien, kein `git add -p`).
- **AP-Diagramm 3-spaltig** via unsichtbarer Mermaid-Links (`~~~` intra- und inter-Subgraph). Siehe Insight §2.
- **Doku-Versionsanzeige** lebt in icon-rail.js/zensical.toml — getrennt von `config.APP_VERSION`. Siehe Insight §1.

## Git-Status
- Branch: `master` (sauber, alles committet)
- origin/master: `16170e1` (inkl. Handoff + Poster) · origin/gh-pages: `227f72b` — beide gepusht
- Uncommitted: keine · Stash: keiner
- Version: **0.3.1** · Tests: **111 grün**
- Live-Doku: https://monoeagle.github.io/lucent-job-luDBxP/

## Laufende Prozesse
- Dev-Server läuft im Hintergrund auf http://127.0.0.1:5057 (meldet 0.3.1). Bei neuer Session ggf. neu starten: `pkill -f "python app.py"; ./venv/bin/python app.py` (persistent: run_in_background, nicht `&`).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit Lucent DB Explorer (luDBxP).
Letzte Session: 2026-06-26 02:52 — v0.3.1, 111 Tests grün, alles committet + deployed (master 16170e1, gh-pages 227f72b).
Kontext: Visueller Join-Pfad-Builder (Flask). AP-1…AP-9 fertig; keine offenen APs (todo.md leer).
Nächster Schritt: Auf neue Anforderung warten. Kommunikation auf DEUTSCH. Version pro AP via sync_version.py bumpen (Doku-Stellen icon-rail.js/zensical.toml/make_poster.py manuell mit-bumpen; Server nach Bump neu starten).
Handoff-Datei: docs/handoffs/2026-06-26-0252.md
