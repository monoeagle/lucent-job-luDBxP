# Session Handoff — 2026-06-28 20:52 (Session 12)

## Was wir gemacht haben
Sechs Releases am Stück (v0.43.4 → **v0.46.0**), durchgängig per SDD (Brainstorm → Spec → Plan →
Subagent-Tasks → Reviews → opus-Final-Review → Browser-Smoke/Screenshot → Release/Deploy). Zuerst den
**SQL-Builder-UI-Block AP-E/AP-F vollendet** (damit AP-A…F komplett), dann zwei Layout-Fixes
(AP-58/AP-59) und die **Connection-Form-Ausrichtung** (AP-60), schließlich die erste **Migrations-Scheibe
AP-54** (Cross-Schema-FK-Diagnose). Dazwischen den **gesamten Backlog sauber dokumentiert**
(AP-55/56/57 Migration, AP-61/62, AP-63·S1-3) inkl. zwei Konzept-Dokumente.

## Erledigt (alles live auf origin/master + gh-pages)
- [x] **AP-E** — SQL-Builder Zeilen-Move ↑/↓ (ORDER BY + Spalten; Buttons, gestaged) + Legenden-Fix (1-N linksbündig) — **v0.44.0**
- [x] **AP-F** — SQL-Analyzer „Optimierungs-Vorschläge" (4 schema-freie AST-Heuristiken, eigene Kategorie) — **v0.45.0**
- [x] **AP-58** — Fix HAVING-Layout (Sektion hatte kein eigenes CSS; fluchtet jetzt wie Filter/Sortierung/Spalten) — **v0.45.1**
- [x] **AP-59** — SQL-Builder 2-Spalten-Raster (Sektions-Label = „+ Label"-Button, erste Zeile auf gleicher Linie) — **v0.45.2**
- [x] **AP-60** — Connection-Form sauber ausgerichtet (feste Label-Spalte + einheitliche Feldbreite, alle DB-Typen) — **v0.45.3**
- [x] **AP-54** — Cross-Schema-FK-Diagnose (read-only): `ref_schema` im Model, `Schema.cross_schema_fks()`, `/api/schema`, Info-Panel Count+Kantenliste — **v0.46.0**
- [x] **Backlog dokumentiert:** AP-55/56/57 (Migrations-Konzept), AP-63·S1-3 (Objekt-Kategorien-Konzept), AP-60/61/62 — in Roadmap/Gantt/Board namentlich + 2 Konzept-Dokumente unter `docs/concepts/`
- [x] Tests **308 → 329** (2 skipped); Doku je AP laufend nachgezogen (keine Drift)

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (laut empfohlener Reihenfolge):** **AP-55 — Implied-FK-Schärfung** (Aufwand M). `core/implied.py`
  (heute: Spaltenname-vs-PK-Matching, `include_implied`) um **konfigurierbare Namensmuster** (`<Entity>ID`/`_ID`/`UUID`)
  + **Confidence-Score** erweitern; in der UI klar „heuristisch, kein FK"; profitiert von AP-54 (Schema-Tagging),
  aber nicht zwingend. Tests gegen konstruierte Schemas (Treffer + bewusste Nicht-Treffer). → Brainstorm zuerst.
- [ ] Danach laut Reihenfolge: **AP-56** (Subset-Export, L) — größter Migrationsnutzen.
- [ ] Vorgeschlagene Gesamt-Reihenfolge siehe unten (Entscheidungen).

## Entscheidungen dieser Session
- **Reihenfolge des offenen Backlogs (empfohlen, an der HCMX-Migration ausgerichtet):**
  Wave 1 Quick-Wins (AP-60 ✓), Wave 2 Migration (**AP-54 ✓ → AP-55 → AP-56**), Wave 3 Tool-Tiefe (AP-63·S1/S2, AP-61),
  Wave 4 bedingt (AP-63·S3, **AP-57 nur wenn AP-54 echte Cross-Schema-FKs findet**). Infra (AP-19/31/34/35) quer, niedrige Dringlichkeit.
- **AP-54 als billiges Gate für AP-57:** Statt direkt die XL-Cross-Schema-Join-Stufe zu bauen, erst die S-Diagnose —
  sie beantwortet empirisch, ob die Ziel-DB überhaupt Cross-Schema-FKs hat. Insight `2026-06-28-cheap-diagnostic-gates-expensive-feature.md`.
- **HCMX-Kernerkenntnis (prägt AP-55/56):** Produkt-übergreifende Links der Alt-Suite sind **fachliche IDs, keine FKs** —
  daher ist *nicht* Cross-Schema-Join der Hebel, sondern implied-FK-Erkennung (AP-55) + referenziell konsistentes Subsetting (AP-56).
  Konzept: `docs/concepts/2026-06-28-legacy-db-migration-tooling.md`.
- **Passwort-Persistenz (AP-62):** Passwort wird bewusst NICHT gespeichert (Whitelist `routes.py:_CONN_FIELDS` ohne `password`,
  Klartext-JSON). Nur bauen, wenn Persistenz gewünscht → dann via OS-Keyring, nicht Klartext.
- **AP-58/AP-60 inline (ohne SDD-Subagenten):** kleine, klar diagnostizierte CSS-Fixes wurden als Controller inline gebaut
  + per Browser-Screenshot verifiziert — proportional zum Aufwand.

## Git-Status
- Branch: `master` — synchron mit `origin/master` (0/0), Working Tree **clean**
- HEAD: `c1d7df5`; gh-pages remote `ace35ff`
- Version: **0.46.0**; Tests **329 passed, 2 skipped**
- Uncommitted: keine (dieser Handoff + Insight + KPI-Zeile werden mit committet) · Stash: keiner

## Wichtig für die nächste Session
- App-Start: `bash run.sh --tray`. venv = Python 3.14; Tests `./venv/bin/python -m pytest`.
- **UI-Verifikation:** Browser-Smoke via Playwright/System-`python3` (Vorlagen `.superpowers/sdd/verify_*.py`); bei
  **Template-/Route-Änderungen App neu starten** (`LUCENT_PORT=5057 bash run.sh --skip-setup`), JS/CSS sind live.
- Je AP/Feature: Version-Bump (`sync_version.py` + icon-rail `APP_VERSION`/`TEST_COUNT` **manuell**) + Changelog/Mirror +
  Roadmap/Board/Gantt (jedes Item namentlich, Offen→Erledigt verschieben) + zensical.toml + Site + gh-pages-Deploy; Deutsch/NO-CDN; SDD-Final-Review nie weglassen.
- **Offener Backlog (8 APs):** AP-19/31/34/35 (Infra) · AP-55/56/57 (Migration) · AP-63·S1/S2/S3 (Objekt-Kategorien) · AP-61/62 (Demo/Passwort).
- `favicon.ico`-404 im Browser-Smoke ist vorbestehend/benign.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit LucentTools DB Explorer.
Letzte Session: 2026-06-28 20:52 (Session 12), Stand **v0.46.0, 329 Tests grün** (2 skipped), alles nach origin/master + gh-pages gepusht, github.io live, Working Tree clean.
ZUERST: git fetch + Divergenz prüfen; `./venv/bin/python -m pytest` als Baseline (venv = Python 3.14). App via `bash run.sh --tray`.
Kontext: Reverse-Engineering-/Migrations-Tool für die Ablösung von OpenText HCMX (Oracle/MSSQL-DBs) — Alt-DB verstehen → sauberer Export → neues OpenTofu-Modell.
Stand: SQL-Builder-UI-Block AP-A…F komplett; AP-54 Cross-Schema-Diagnose fertig (Gate für AP-57). Gesamter Backlog dokumentiert (Roadmap/Gantt/Board + `docs/concepts/`).
Nächster Schritt: **AP-55 — Implied-FK-Schärfung** (M) — `core/implied.py` um konfigurierbare Namensmuster + Confidence-Score erweitern (Hebel für HCMX-Cross-Produkt-Links); Brainstorm zuerst. Danach AP-56 (Subset-Export, L).
Merke: je Feature Version-Bump (sync_version.py + icon-rail APP_VERSION/TEST_COUNT) + Roadmap/Board/Gantt (Offen→Erledigt) + zensical + gh-pages; Deutsch/NO-CDN; SDD-Final-Review nie weglassen; UI-Browser-Smoke prüft *gerenderte* Eigenschaften (CSS-Spezifität!) und braucht App-Neustart bei Template/Route-Änderung; AP-57 nur bauen, wenn AP-54 echte Cross-Schema-FKs nachweist.
Memory: [[ludbxp-sqlbuilder-ui-block]], [[ludbxp-release-deploy-steps]], [[ludbxp-ap51-carryover]].
Handoff-Datei: docs/handoffs/2026-06-28-2052.md
