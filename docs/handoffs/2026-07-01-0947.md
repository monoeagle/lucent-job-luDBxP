# Session Handoff — 2026-07-01 09:47 (Session 17)

## Was wir gemacht haben
Acht Releases (v0.60.0 → v0.65.1): die **AP-65-Analyzer-Serie abgeschlossen** (A-Härtung 2 · Gutter · Lints mit Zeilenbezug), **AP-67·Oracle-Adaption** live gebaut (kompakter Seed → **reiches ~37-Tabellen-Schema**, Slice 2a), und **drei nutzer-/live-gefundene Dialekt-Bugs** gefixt. Ein echter Oracle-21c-XE-Container (podman) fand fünf reale Bugs, die grüne Tests nie sahen.

## Erledigt
- [x] **v0.61.0 — AP-65·A-Härtung 2:** Unclosed-Quote → echte Fehlerzeile via Ungerade-Quote-Heuristik (`core/sqlanalyze.py::_odd_quote_line`); Redirect ohne Spalte/Mark, Hint nennt Zeile.
- [x] **v0.62.0 — AP-65·B:** Zeilennummern-Gutter + Fehlerzeilen-Highlight (3-Schicht-Editor `attachLineGutter`, NO-CDN); Eingabefeld bleibt verstellbar (Grip-Fix).
- [x] **v0.63.0 — AP-65·C:** Lints mit Zeilenbezug — `_node_line` (sqlglot `meta['start']`) + `line`-Feld an `AnalysisWarning`/`AnalysisSuggestion`; „Zeile N:"-Präfix + Klick markiert die Zeile. **AP-65 komplett.**
- [x] **v0.64.0 — AP-67·Oracle-Adaption:** Oracle-Zweig des Seeders (kompakte 5-Tab-CMDB + alle Oracle-Objekte), skip-guardeter Live-Test, README. Live gegen Oracle 21c XE.
- [x] **v0.64.1 — Fix:** Passwort-Abfrage beim Verbinden gespeicherter Passwort-DBs (`connectSaved`/`connectSavedWith`).
- [x] **v0.64.2 — Fix:** dialekt-korrekte Daten-Vorschau (`fetch_rows` via SQLAlchemy Core → FETCH FIRST + Casing).
- [x] **v0.65.0 — AP-67 Slice 2a:** reiches Oracle-Schema — `sample_data/oracle_demo.py` (37 Tab · 10 Views · 16 Routinen/4 Packages · 3 Seq · 2 Matview · 3 Synonyme, reicher FK-Graph: Diamanten/Composite-FK/2 Self-Refs/No-Path/alt. Routen). `CREATE`-dann-`ALTER-FK`-Muster, idempotent, live verifiziert.
- [x] **v0.65.1 — Fix:** Oracle-Bezeichner-Casing im generierten SQL (`Dialect.quote()` quote-if-needed via SQLAlchemy-Preparer) — **eine** Stelle reparierte Join/Distinct/Subset; Offline-Regressions-Test.
- [x] Tests **446 passed / 11 skipped** (436→446). Alles nach origin/master (`73c429e`) + gh-pages (`c49b291`) deployt, github.io live. Doku je Release grep-verifiziert; Handoff-Doku-Drift (`oracle_demo.py` in projektstruktur.md) gefixt. Insight + KPI-Zeile #17 geschrieben.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (frei wählbar):**
  - **AP-67 Slice 2b — Offline-Fixture-Vorschau** (der ursprüngliche „ohne DB sehen"-Wunsch): `Schema.to_dict/from_dict` (core, pur, round-trip-testbar) + Snapshot aus der reichen Live-Reflektion + Fixture-Loader (`manual_loader`-Stub beleben) + **Load-Dispatcher** über die ~14 `SqlAlchemyLoader(url).load()`-Aufrufstellen in `web/routes.py` + gespeicherte „Oracle-Vorschau"-Verbindung (Sentinel-URL) + **Graceful-No-Exec** in den Ausführungs-Endpoints (run/preview/distinct/subset). Eigene Brainstorm-Runde. Datenquelle (reiches Schema) steht.
  - **AP-66·S2/S3** (M/XL) — Routinen-Quelltext aus View verlinken / PL/SQL-Daten-Lineage.
  - **Cleanup-AP** (S) — Minors aus den Reviews (doppelter `usp_vm_count`-Drop in `oracle_demo.py`; 2 inerte Demo-fns; optionales TOOLS-Anpinnen in der Sidebar; Loader `get_indexes()` 2×/Tabelle cachen).
  - **AP-57** (bedingt) — Cross-Schema-Joins.
- [ ] Bei jedem Feature: Brainstorm → Spec → Plan → SDD; je Release Version-Bump + volle Doku (Changelog EN/DE, Roadmap/Gantt/Board, CLAUDE.md, Kennzahlen inkl. **Per-Modul-Balken**, Site, gh-pages).

## Entscheidungen dieser Session
- **Reiches Oracle-Schema live entwickelt** (Controller), nicht subagent-transkribiert — 37-Tab-DDL entsteht gegen die Container-Ground-Truth; `CREATE`-dann-`ALTER-FK` vermeidet Erzeugungs-Reihenfolge-Kopplung (lief beim 1. Versuch mit 0 Fehlern).
- **Dialekt-/Casing-Bugs an der geteilten Quelle fixen:** `Dialect.quote()` einmal → Join+Distinct+Subset (Insight 2026-07-01).
- **Oracle-Casing-Round-Trip:** reflektierte lowercase-Namen **unquoted** rendern (SQLAlchemy quote-if-needed), nicht quoted — sonst ORA-00942.
- **AP-67 in Scheiben:** Slice 1 (kompakt) → 2a (reich) → 2b (offline), je releasebar; „beides jetzt" via Sequenz statt einem großen Push.
- **Live-Tests echt fahren:** der Oracle-Container bleibt laufen; er fand 5 reale Bugs.

## Offene Follow-ups (Backlog, nicht-blockierend)
- **AP-67 Slice 2b** (s. o.) — der eigentliche Offline-Vorschau-Mechanismus.
- **Cleanup:** doppelter `usp_vm_count`-Drop; inerte `fn_cluster_vm_count`/`fn_datastore_capacity` (gewollt); `oracle_demo.py`-Tabellen ohne `PURGE` (Recycle-Bin, kosmetisch); Connect-Prompt zeigt Passwort im Klartext (prompt(), lokal ok).
- **Live-Verifikation:** AP-65/66/67-Pfade gegen **PG/Oracle** breiter (Oracle jetzt reich verifiziert; PG braucht Instanz).

## Git-Status
- Branch: `master` (= origin/master `73c429e`); `gh-pages` = origin `c49b291`
- Uncommitted: der projektstruktur.md-Drift-Fix + Insight + Handoff + KPI-Zeile werden noch committet
- Stash: none; alle 8 Feature/Fix-Branches gemergt (FF) + gelöscht.
- **Laufende Prozesse:** App v0.65.1 auf :5057 (`run.sh --skip-setup`); **Oracle-Container `oracle-luDBxP`** (gvenzl/oracle-xe:21-slim-faststart, `demo/demo @ localhost:1521/XEPDB1`) — reiche Demo geseedet; gespeicherte „Oracle-Demo"-Verbindung in der App.

## Verifikation
- `./venv/bin/python -m pytest` → **446 passed, 11 skipped** · Stand **v0.65.1**
- Oracle live: reiche Demo reflektiert (37/10/16), SQL-Builder-Multi-Join läuft, Daten-Vorschau/Distinct/Subset laufen; skip-guardeter Oracle-Seed-Test PASSED mit URL. github.io live mit v0.65.1.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit LucentTools DB Explorer.
Letzte Session: 2026-07-01 09:47 (Session 17), Stand v0.65.1, 446 Tests grün (11 skipped — Oracle/MSSQL/PG-Live), alles nach origin/master (`73c429e`) + gh-pages (`c49b291`) gepusht, github.io live, Working Tree clean. Doku je Release grep-verifiziert.
ZUERST: git fetch + Divergenz prüfen; `./venv/bin/python -m pytest` als Baseline (venv = Python 3.14). App via `bash run.sh --skip-setup` (Tray-GUI crasht headless) — vor Live-Demo `/api/info`-Version prüfen + neu starten (alte Instanz killen `ss -ltnp|grep 5057`). **Oracle-Container `oracle-luDBxP` läuft** (gvenzl/oracle-xe:21-slim-faststart, demo/demo @ localhost:1521/XEPDB1) mit der reichen Demo; gespeicherte „Oracle-Demo"-Verbindung braucht beim Verbinden das Passwort `demo` (Prompt). Echte Live-Tests fahren statt nur skip-guarded (Insights 2026-06-30 + 2026-07-01).
Kontext: Reverse-Engineering-/Migrations-Tool für die HCMX-Ablösung (Oracle/MSSQL). Alt-DB verstehen → referenziell sauberer Export → neues OpenTofu-Modell.
Stand: **AP-65 komplett** (Analyzer Fehlerzeile/Gutter/Lints-mit-Zeilenbezug); **AP-67** Oracle-Adaption (Slice 1 kompakt + Slice 2a **reiches ~37-Tab-Schema**) ✓; 3 Oracle-Dialekt-Bugs (Connect-Passwort, Daten-Vorschau-LIMIT/Casing, generiertes-SQL-Casing) gefixt.
Nächster Schritt (frei wählbar): **AP-67 Slice 2b — Offline-Fixture-Vorschau** (Schema `to_dict/from_dict` + Snapshot aus reicher Reflektion + Fixture-Loader [manual_loader-Stub] + Load-Dispatcher über ~14 `SqlAlchemyLoader.load()`-Stellen in `web/routes.py` + „Oracle-Vorschau"-Verbindung + Graceful-No-Exec in run/preview/distinct/subset) — Datenquelle steht. Brainstorm zuerst. ODER **AP-66·S2/S3** ODER **Cleanup-AP** (Review-Minors) ODER **AP-57** (Cross-Schema, bedingt).
Merke: je Feature Version-Bump (sync_version.py) + Changelog EN/DE + Roadmap/Board/Gantt-`.mmd` + zensical + Kennzahlen (Headline + **Per-Modul-Balken** hartkodiert!) + Site + gh-pages; Deutsch/NO-CDN; SDD-Whole-Branch-Review (opus) nie weglassen; Subagenten modell-gestuft (haiku/sonnet/opus); UI-Smoke braucht App-Neustart bei Route/Template/Python-Änderung; **beim Anbinden eines Backends JEDEN generierten-SQL-Pfad live fahren** (Preview/Join/Distinct/Subset), Dialekt-/Casing-Bugs an der geteilten Quelle fixen (Insight 2026-07-01).
Handoff-Datei: docs/handoffs/2026-07-01-0947.md
