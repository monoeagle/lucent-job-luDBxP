# Session Handoff — 2026-06-26 15:30

## Kontext
**Lucent DB Explorer** (lucent-job-luDBxP) — Flask-Tool für Join-Pfade über
DB-Schemas (FK-Graph → read-only SQL), 3-Panel-UI wie ein minimalistischer SQL
Developer. Diese Session lief auf **Windows** (Linux-Arbeiten ausdrücklich für
abends auf einem anderen Rechner verschoben).

## Wichtig: Remote-Divergenz zu Session-Beginn
Der lokale Stand war **v0.3.0** (Snapshot `9d55a9c`), das Remote aber bereits
**v0.3.1** (eine „Session 2": Poster, AP-9 = „Ergebnisliste maximiert"). Das fiel
erst beim Push auf → Rebase + **Umnummerierung** der geplanten APs nötig (AP-9 war
remote schon anders vergeben). **Lehre fürs nächste Mal:** vor jeder Arbeit
`git fetch` + Divergenz prüfen (als Memory festgehalten).

## Erledigt diese Session (v0.3.1 → v0.10.0, 17 Commits, 118 Tests grün)
- [x] **AP-14 (Windows)** — Python-3.14-Readiness: Offline-Wheelhouse von cp312 auf
      **cp314** umgestellt (alle 5 kompilierten Pakete in identischer Version),
      `run.ps1`/`run.sh`-Gate auf 3.14; Python 3.14.6 via winget; 111 Tests grün. v0.4.0.
      Wheel-Optimierungen (greenlet weg / SQLAlchemy none-any) **geprüft & verworfen**.
- [x] **AP-11** — Composite Foreign Keys **voll** unterstützt: FK trägt alle Spaltenpaare
      (`column_pairs`), `JOIN … ON a.x=b.x AND a.y=b.y`; getrennte Mehrfach-FKs bleiben
      alternative Routen (`JoinEdge`). End-to-end verifiziert. v0.5.0.
- [x] **AP-10** — Gespeicherte Verbindungen in der **Topbar** (Dropdown, Direkt-Connect,
      Sync mit Verbindungs-Tab). Playwright-verifiziert. v0.6.0.
- [x] **AP-13** — UI-Politur: Suchfeld im Objekt-Browser, linker Sidebar-Splitter,
      „Neu anordnen"-Button + adaptives Graph-Layout. Playwright-verifiziert. v0.7.0.
- [x] **AP-15 (nur `run.ps1`/Windows)** — abbruchsicher + idempotent: Prereq-Check je
      Schritt, venv-Integrität, selbstheilender Install, **NO-CDN** (strikt `--no-index`
      aus `wheels\` + `--dry-run`-Vorabprüfung mit Protokoll, kein Online-Nachladen),
      Port-Check, Status-Helfer. 4 Szenarien verifiziert. v0.8.0.
- [x] **AP-12 (nur Backend)** — MSSQL: ODBC **Driver 18** default + `Encrypt`/
      `TrustServerCertificate` konfigurierbar; **klare Treiber-Fehlermeldung** statt roher
      pyodbc-Exception. Doku ergänzt. v0.9.0.
- [x] **AP-20** Copy-Icon am SELECT (Clipboard) + **AP-21** Kosmetik gleiche Balkenhöhe.
      Playwright-verifiziert. v0.10.0.
- [x] **Backlog dokumentiert** (nur Doku, in `todo.md`): AP-16…AP-25.

## In Arbeit / Nächste Schritte
**Heute Abend auf Linux:**
- [ ] **Doku-Site bauen** — alle Quellen (AP-Diagramm `.mmd`, `datenmodell.md`,
      `oberflaeche.md`, `installation.md`) sind aktualisiert; nur der Zensical-Build
      (+ Mermaid-SVG) fehlt. Build **nur unter Linux** (Konvention).
- [ ] **AP-15 `run.sh`** — Parität zu `run.ps1` spiegeln (venv-Integrität, idempotent,
      Status, Port-Check via `ss`/`lsof`, `|| true` entfernen). **Offene Architekturfrage:**
      NO-CDN auf Linux braucht ein **Linux-Wheelhouse** (manylinux-cp314) — die `wheels/`
      sind win_amd64. Quelle/Strategie entscheiden.
- [ ] **AP-14-Rest** — AppImage/3.14 auf Linux bauen (`run.sh _bundle_python_standalone`).
- [ ] **AP-12-Rest** — realer Integrationstest gegen MSSQL-Instanz + UI-Felder für Encrypt/Trust.

**Windows-tauglich (offener Backlog):**
- [ ] **AP-23** Join-Builder-Maske vereinheitlichen (Buttons/Dropdowns gleich groß, ggf. Redesign)
- [ ] **AP-16** Graph entzerren (Crossing-Reduction-Layout, z. B. dagre/elk)
- [ ] **AP-22 (Frage)** implizite FKs default aktivieren? · **AP-24 (Frage)** Session-KPIs
- [ ] **AP-25** SQL-Statement-Analyzer (großes Feature, read-only; Brainstorm im Ticket)
- [ ] **AP-17** Delivery-Verzeichnis ohne AI-Spuren · **AP-19** `.pattern_transfer`

## Entscheidungen / Konventionen (als Memory festgehalten)
- **Sprache:** alle Kommunikation auf Deutsch.
- **Doku-Site nur Linux** bauen; Windows = nur App.
- **Vor Arbeit immer `git fetch`** + Remote angleichen (Divergenz-Lehre).
- **Versionierung:** 1.0.0 **nur auf Ansage**; bis dahin Feature → minor, Fix → patch.
- **NO-CDN / nur lokale Sourcen** auch beim Setup (run.ps1 strikt offline).
- **Playwright + Chromium** wurden ins venv installiert (für UI-Verifikation) — **nicht**
  in `requirements*.txt`, nicht committet; bei `run.ps1 clean` weg, problemlos neu via
  `pip install playwright` + `playwright install chromium`.

## Git-Status
- Branch `master`, sauber, synchron mit `origin/master` (zuletzt `b048464`).
- 69 Commits gesamt, Version **0.10.0**, **118 Tests grün**.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit Lucent DB Explorer (lucent-job-luDBxP). Letzte Session: 2026-06-26 15:30.
ZUERST `git fetch` + Divergenz prüfen (Remote war diese Session mehrfach voraus!).
Stand: v0.10.0, 118 Tests grün, Windows-APs großteils erledigt (AP-10/11/13/20/21 komplett;
AP-12/14/15 nur Windows-/Backend-Teil). Offen v. a. auf Linux: Doku-Site bauen, AP-15 run.sh
(+ Linux-Wheelhouse-Frage), AP-14 AppImage, AP-12 realer MSSQL-Test. Windows-Backlog:
AP-23 (Join-Builder-Maske), AP-16 (Graph entzerren), AP-25 (SQL-Analyzer), Fragen AP-22/AP-24.
Konventionen: Deutsch · Doku-Build nur Linux · 1.0.0 nur auf Ansage · NO-CDN/nur lokale Sourcen.
Handoff: docs/handoffs/2026-06-26-1530.md
