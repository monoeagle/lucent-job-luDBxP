# Session Handoff — 2026-06-27 20:37 (Session 8)

## Was wir gemacht haben
**LucentTools DB Explorer** (lucent-job-luDBxP), Linux. Diese Session ging von **v0.31.0 (200 Tests)**
auf **v0.34.0 (227 Tests)**: AP-45 fertiggestellt, dann die beiden lange offenen **nicht-UI-Themen
AP-31 (Multi-User) und AP-34 (Tray-Launcher)** als Kern-Scheiben **per Subagent-Driven Development**
gebaut, dazu viel Doku-/Diagramm-/Screenshot-Arbeit und ein dev-internes **Projekt-Kennzahlen-Dashboard**.

## Erledigt (v0.31.0 → v0.34.0)
- [x] **AP-45** (v0.32.0) — Spaltenkopf-Aktionen in der Ergebnistabelle (Sortieren/Filter/Spalte entfernen) + Filter-Wertfeld mit echten DISTINCT-Werten (`/api/distinct`) + `columns_meta`. Inline gebaut (TDD), 12 Playwright-Checks.
- [x] **v0.32.1** — Filter sofort wirksam (Auto-Rebuild) + DISTINCT-Race-Fix; Doku „zwei DISTINCT" abgegrenzt.
- [x] **Oberfläche-Screenshots** alle 7 neu erzeugt (aktueller UI-Stand; Join-Builder zeigt komplexe 6-Hop-Variante).
- [x] **AP-Board** lesbarer: zweizeilige Kacheln (`AP-N` / Stichwort), Join-Builder als 5×5-Gitter, vollbreit; fehlende Abhängigkeitspfeile AP-31→AP-34, AP-33→AP-34 ergänzt.
- [x] **AP-31 (Kern)** (v0.33.0) — dynamischer Port (5057 bevorzugt, sonst frei; `LUCENT_PORT`) + Pro-Nutzer-`config.json`/Logs (`core/userpaths.py`, XDG/`%LOCALAPPDATA%`, Migration). **SDD** (6 Tasks + Final/opus); Final-Review fand den Launcher-Abbruch-Bug → gefixt. Betriebsseite „Terminalserver" ergänzt.
- [x] **AP-34 (Kern)** (v0.34.0) — Tray-Icon-Launcher (`launcher/`, pystray/Pillow): Ein-Klick-Start via `run.ps1 -Action tray`/`run.sh --tray` (venv-Bootstrap), fensterlos, Menü Im-Browser-öffnen/Info/Beenden, Auto-Browser, sauberes Beenden = Port frei. **SDD** (3 Tasks + Final/opus + Fix T1a).
- [x] **docs/projekt-kennzahlen.html** — dev-internes Dashboard (nach LibreDrive-Vorbild): 98 % Umsetzung, 89 % Coverage, 93 %/64 % Doku, COCOMO, LOC.

## In Arbeit / Nächste Schritte
- [ ] **Kein dringender offener Strang.** Einzig offenes Produkt-AP: **AP-35** (Windows-only) — `run.ps1`: leeres venv gilt fälschlich als „vollständig" (Fix wie in `run.sh`, dann signiertes Skript re-signieren). Nur unter Windows test-/baubar.
- [ ] **AP-31-Rest** (eigene Scheiben): lokaler WSGI-Server (waitress), Idle-Shutdown/sauberer Stop, Verteilung/Härtung.
- [ ] **AP-34-Rest** (eigene Scheiben): Live-Log-Fenster (Tkinter, Bezug AP-33); automatisches Ausrollen der Startmenü-/Desktop-Verknüpfung; **Tray-GUI live auf Windows/Desktop verifizieren** (headless hier nur Import/Build geprüft, `.run()` nicht).
- [ ] **AP-19** (`.pattern_transfer`) = übergeordnetes Meta-/Destillat-AP, kein Produkt-AP.

## Entscheidungen dieser Session
- **AP-34 Variante A (Python/pystray)** statt PowerShell/.NET; **kein `.exe`-Bau** → Auslieferung über `run.ps1`-venv-Bootstrap (PowerShell erlaubt, Re-Signatur akzeptiert). Subprozess-Modell (Launcher startet `app.py`).
- **AP-34 Info zeigt Version/URL/Port** statt „aktive Verbindung" (server-seitig keine globale aktive Verbindung, read-only).
- **AP-31 Pro-Nutzer-Pfade immer** (Slug `luDBxP`, OS-Standard) + dynamischer Port (5057 bevorzugt). Launcher brechen bei belegtem Port nicht mehr ab.
- **Kennzahlen-AP-Zählung:** AP-17 (gestrichen) und AP-19 (Meta/Destillat) **nicht** als Produkt-APs gezählt; AP-31/34-Kerne = fertig.
- Insight: **Final-Whole-Branch-Review fängt Lücken außerhalb des Task-Diffs** (`docs/insights/2026-06-27-final-review-faengt-luecken-ausserhalb-des-diffs.md`).

## Git-Status
- Branch `master`, **synchron mit origin/master** (vor diesem Handoff-Commit `720173c`).
- Uncommitted: dieser Handoff + Insight + KPI-Zeile + `docs/projekt-kennzahlen.html` (gleich committen).
- Stash: keine. Version **0.34.0**, **227 Tests grün**, 1 skipped (MSSQL).
- SDD-Branches (feature/ap31-…, feature/ap34-…) gemergt (FF) + gelöscht. `.superpowers/sdd/` ist gitignored.

## Umgebung / Konventionen
- venv = Python 3.14 (uv). App: `bash run.sh` (5057, sonst freier Port) bzw. `bash run.sh --tray`.
- **Nach Backend-Änderungen (Python) App neu starten** — JS/CSS/Templates live.
- Doku-venv `luDBxP-docs/.venv-docs`, Site-Build nur Linux; gh-pages = manuelles Worktree-Deploy.
- Deutsch · NO-CDN · vor Arbeit `git fetch`+Divergenz · je AP committen (ohne KI-Signatur) · 1.0.0 nur auf Ansage · Release berührt mehr als sync_version.py (Badges/zensical/Changelog-Mirror/Diagramme/Betriebsseite).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit LucentTools DB Explorer (lucent-job-luDBxP), Linux.
Letzte Session: 2026-06-27 20:37 (Session 8), Stand **v0.34.0, 227 Tests grün** (1 skipped), alles nach origin/master + gh-pages gepusht, github.io live.
ZUERST: `git fetch` + Divergenz prüfen; `./venv/bin/python -m pytest` als Baseline (venv = Python 3.14).
Stand: alle Produkt-APs außer **AP-35 (Windows-only)** fertig; AP-31/AP-34 Kerne fertig (Reste = eigene Scheiben). Kein dringender offener Strang.
Mögliche nächste Schritte: AP-34-Rest (Live-Log-Fenster Tkinter / Tray-GUI auf Windows verifizieren / Verknüpfungs-Ausrollen) · AP-31-Rest (waitress/Idle-Shutdown) · AP-35 (nur Windows).
Merke: nach Python-Änderungen App neu starten; je AP Version-Bump + gh-pages-Deploy; Deutsch/NO-CDN; SDD-Final-Review niemals weglassen (fängt Lücken außerhalb des Diffs).
Handoff-Datei: docs/handoffs/2026-06-27-2037.md
