# Session Handoff — 2026-07-02 00:26 (Session 18)

## Was wir gemacht haben
Drei Releases live+deployt (v0.66.0 → v0.67.0): **AP-68 Roadmap-Swimlane** (eigener Python-SVG-Generator statt pro-AP wachsendem Gantt), dessen **Polish** (v0.66.1), und **AP-69·A gemeinsame SQL-Editor-Komponente** (v0.67.0, dunkler 3-Schicht-Editor mit Zeilennummern für alle SQL-Flächen). **AP-67·2b (Offline-Fixture-Vorschau) verworfen** (off-mission). Ein Live-Chrome-Fund deckte eine Render-Regression auf (Fehlerzeilen-Text unsichtbar) — Fix committet, aber noch nicht released.

## Erledigt
- [x] **v0.66.0 — AP-68 Roadmap-Swimlane:** eigener stdlib-Python-SVG-Generator (`luDBxP-docs/tools/generate_roadmap_svg.py`, Datenquelle `roadmap_data.py`), 10 Themen-Lanes konstanter Höhe, erledigte Historie verdichtet, offene APs als Marker; altes Gantt archiviert; SDD (6 Tasks) + Whole-Branch-Review; live+deployt.
- [x] **v0.66.1 — AP-68 Polish:** Titel zentriert, Anzahl erledigter APs im Balken, 🚧-Marker, Anti-Overlap (mehrstufig + Rechtsrand-links + weißer Halo). Whole-Branch-Review fand eine echte Regression (2. Einbettungspfad `icon-rail.js` → gelöschte Alt-SVG) → gefixt. live+deployt.
- [x] **v0.67.0 — AP-69·A SQL-Editor-Komponente:** `sqlEditor({value,readOnly,rows})` verallgemeinert `attachLineGutter`; Analyzer editierbar (Fehlerzeile), generiertes SQL/Detail-„SQL"-Tab/Subset read-only; `.an-*`→`.sqled-*`. SDD (5 Tasks) + Whole-Branch-Review (Ready YES). live+deployt. **459 Tests grün.**
- [x] **AP-67·2b verworfen:** Nutzer: „kein Snapshot; Tool verbindet sich mit echten DBs." Memory `ludbxp-no-offline-snapshot` + Doku-Verweise (roadmap.md, Konzept-Dok) als verworfen markiert.
- [x] **icon-rail-Badge-Nachzug** (v0.66.0 war auf 0.65.1/446 stehengeblieben) → jetzt Teil der Release-Checkliste.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT — v0.67.1 entscheiden:** der Fix `fix(sql-editor): Fehlerzeilen-Text sichtbar (z-index) + hellere Schrift` liegt committet auf Branch **`fix/ap69-error-line-visibility`** (`21472ec`), **nicht** released/gepusht. Live verifiziert (Screenshot). Entscheiden: **so als v0.67.1 rausbringen (Version-Bump + Changelog EN/DE + icon-rail APP_VERSION 0.67.1 [TEST_COUNT bleibt 459] + kennzahlen + Site) → merge master → push → gh-pages-Deploy** — ODER vorher der **rote Fehler-Balken heller/kontrastreicher** (Nutzer hatte das als Option offen gelassen; „schwarze Schrift pro Zeile" geht in einer Textarea technisch nicht — helle Schrift auf hellerem Balken ist der Weg).
- [ ] **UI-Serie Rest** (der Nutzer wählte A zuerst; B/C folgen): **B — Sidebar-Tree auf/zuklappbar** (Subzweige einzeln, default zu außer Tools; `renderSidebar()` in app.js) · **C — Doku-Site-Lightbox max. 80% Höhe** (`.adb-lightbox` in `luDBxP-docs/docs/javascripts/icon-rail.js`, damit Text unters Bild passt).
- [ ] **Doku-Schuld (Serien-Drift):** die **Roadmap-Erledigt-Prosa** (`luDBxP-docs/docs/projekt/roadmap.md`) endet bei **v0.60.0** — v0.61.0–v0.67.0 (AP-65-Serie, AP-67, AP-68, AP-69) fehlen in der Prosa-Liste. Ebenso der `kennzahlen.md`-„Umsetzungsstand"-Block (~89 %, AP-1…63). Nachziehen.
- [ ] Weiter offen aus dem Backlog: **Windows-Blocker** (cryptography/py3.14 free-threaded vs. abi3-Wheel), **AP-66 S2/S3** (Views→Routinen-Lineage), **AP-62 Keyring**, **AP-57** (Cross-Schema, bedingt).

## Entscheidungen dieser Session
- **AP-67·2b verworfen** — kein Offline-Snapshot; das Tool ist zum Verbinden+Untersuchen echter DBs da (Memory `ludbxp-no-offline-snapshot`).
- **Roadmap = eigener SVG-Generator**, nicht Mermaid (Mermaid-Gantt kann keine echten Swimlanes; jeder Task = eine Zeile).
- **SQL-Editor einheitlich dunkel**, read-only gedimmt (nicht hell/grau).
- **Release-Checkliste erweitert:** Version-Bump berührt auch **`icon-rail.js` APP_VERSION/TEST_COUNT** (war zweimal stehengeblieben) + der 2. Roadmap-Embed-Pfad im Header-Modal.
- **CSS-Layout-Invarianten beim Komponenten-Refactor mit-migrieren** (Insight 2026-07-02): `position/z-index` ging verloren → Fehlerzeilen-Text unsichtbar; kein Test/Diff/Review fing es, nur der Live-Render.

## Git-Status
- Branch: **`fix/ap69-error-line-visibility`** (`21472ec`) — enthält v0.67.1-Fix (CSS z-index/Farbe) + Doku-Drift-Fixes (AP-67·2b verworfen-Markierung, AP-69·A in `roadmap_data.py` + SVG). **Nicht gepusht.**
- `master` = `origin/master` = **`22dcdc4`** (v0.67.0), gepusht; **gh-pages** = `70bfd6d`, deployt, github.io live.
- Uncommitted: clean (alles auf dem Fix-Branch committet).
- Stash: none.
- **Laufende Prozesse:** App v0.67.0 auf `:5057` (`run.sh --skip-setup`), Chrome offen darauf. (Oracle-Container aus Session 17 ggf. noch da; diese Session nicht genutzt.)

## Verifikation
- `./venv/bin/python -m pytest` → **459 passed, 11 skipped** (Stand Fix-Branch `21472ec`; master v0.67.0 identisch grün).
- AP-69·A per Playwright-Smoke je Fläche + Screenshots verifiziert; v0.67.1-Fix (Fehlerzeilen-Text sichtbar + hellere Schrift) live in Chrome bestätigt.
- github.io live mit v0.67.0 (icon-rail `APP_VERSION='0.67.0'`, AP-69·A-Knoten im Flowchart-SVG).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit LucentTools DB Explorer.
Letzte Session: 2026-07-02 00:26 (Session 18), Stand **v0.67.0** (master `22dcdc4` = origin, gh-pages `70bfd6d` deployt, github.io live), **459 Tests grün** (11 skipped Oracle/MSSQL/PG-Live). **Achtung: ein v0.67.1-Fix liegt committet, aber NICHT released, auf Branch `fix/ap69-error-line-visibility` (`21472ec`).**
ZUERST: `git fetch` + Divergenz prüfen; `./venv/bin/python -m pytest` (venv = Python 3.14). App via `bash run.sh --skip-setup` (Tray-GUI crasht headless), vor Live-Demo `/api/info`-Version prüfen + neu starten. UI-JS/CSS sind live (Browser-Reload), aber Route/Template/Python-Änderungen brauchen App-Neustart. **JS-UI immer per Playwright (System-python3) + Screenshot verifizieren — Präsenz-Assertions reichen nicht, die konkrete Interaktion rendern und hinschauen (Insight 2026-07-02).**
Kontext: Reverse-Engineering-/Migrations-Tool für die HCMX-Ablösung (Oracle/MSSQL) — mit echten DBs verbinden und sie untersuchen (KEIN Offline-Snapshot, AP-67·2b verworfen).
Nächster Schritt (SOFORT): **v0.67.1 entscheiden** — den Fehlerzeilen-Sichtbarkeits-Fix auf `fix/ap69-error-line-visibility` als v0.67.1 releasen (Version+Changelog EN/DE+icon-rail 0.67.1 [Tests 459]+kennzahlen+Site → merge master → push → gh-pages) ODER vorher den roten Fehler-Balken heller/kontrastreicher (Nutzer-Option offen). Danach **UI-Serie B** (Sidebar-Tree auf/zuklappbar) + **C** (Doku-Lightbox 80%).
Merke: je Feature Brainstorm→Spec→Plan→SDD; Version-Bump (sync_version.py) + Changelog EN/DE + **icon-rail APP_VERSION/TEST_COUNT** + Roadmap (`roadmap_data.py`-Swimlane + Prosa) + Arbeitspakete-Flowchart `.mmd` + zensical + kennzahlen + Site + gh-pages (Worktree-Deploy: Inhalt von `site/` im Root + `.nojekyll`); Deutsch/NO-CDN; Whole-Branch-Review (opus) nie weglassen; **CSS-Layout-Invarianten beim Refactor mit-migrieren**. **Doku-Schuld:** Roadmap-Erledigt-Prosa endet bei v0.60.0 (v0.61–0.67 nachziehen).
Handoff-Datei: docs/handoffs/2026-07-02-0026.md
