# Session Handoff — 2026-06-28 16:28 (Session 11)

## Was wir gemacht haben
Sieben Releases am Stück (v0.40.0 → **v0.43.4**), durchgängig per SDD (Brainstorm → Spec → Plan →
Subagent-Tasks → Reviews → opus-Final-Review → Browser-Smoke → Release/Deploy). Erst die
**Aggregat-Kette** vollendet, dann der vom Nutzer vorgegebene **UI-Umbau-Block** (AP-A…AP-D) für den
SQL-Builder.

## Erledigt
- [x] **Tier-3 GROUP BY/Aggregate** (v0.41.0) — Auto-GROUP-BY, COUNT/SUM/AVG/MIN/MAX
- [x] **Aggregat-Operationen HAVING + ORDER BY auf Aggregaten** (v0.42.0)
- [x] **COUNT(\*) + COUNT(DISTINCT)** (v0.43.0) — Route unverändert, nur Generator+UI
- [x] **Fix GROUP-BY-Ableitung** (v0.43.1) — Aggregat allein in HAVING/ORDER BY erzwingt nun GROUP BY (systematic-debugging + TDD)
- [x] **AP-A — Umbenennung Join-Builder → SQL-Builder** (v0.43.2) — alles inkl. Bezeichner + Doku; Endpoint `/api/joinpath` bewusst nicht
- [x] **AP-B — Builder-Layout** (v0.43.3) — vier Klausel-Sektionen mit „+" + getrennte Aktionsleiste (Variante C via ASCII-Mockups)
- [x] **AP-C+D — Join-Typ inline + 1-N-Erklärung in Graph-Legende** (v0.43.4)
- [x] Alles nach `origin/master` **und** `gh-pages` deployed (github.io live); Tests 272 → **308** (2 skipped)
- [x] Memory aktualisiert (`ludbxp-ap51-carryover` Aggregat-Kette; neue `ludbxp-sqlbuilder-ui-block`); Insight geschrieben

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (kein offener Strang):** frei wählbar. Der UI-Block hat noch **AP-E** und **AP-F**:
  - **AP-E — Zeilen Move ↑/↓** (Entscheidung steht: Buttons, NICHT Drag&Drop): Verschieben *innerhalb*
    Sortierung (= Priorität) und Spalten (= SELECT-/GROUP-BY-Reihenfolge); zwischen Gruppen kosmetisch.
    UI in `web/static/js/app.js` (`addOrderByRow`/`addColRow` + collect-Funktionen).
  - **AP-F — Analyzer-Optimierungs-Vorschläge**: `core/sqlanalyze.py` um heuristische Vorschläge
    erweitern (hat schon Lints + Komplexitäts-Score). Backend-lastig, unabhängig.
- [ ] **Cross-Schema-Joins** — die große verbleibende Reflection-Stufe (Model/Graph/Pathfinder).
- [ ] **Defer-Minor** (nicht blockierend): `_renderPathList()` wird 2×/Build aufgerufen (redundant, harmlos);
  Fan-out-Warnung-Querverweis-Link nicht in die neue Legende übernommen.

## Entscheidungen dieser Session
- **UI-Block-Zerlegung:** Brain-Dump → AP-A…AP-F logisch geordnet; AP-C+D gebündelt (eine Zone, ein Release) aus Kontext-/Kohärenzgründen.
- **AP-A Umfang:** „alles umbenennen" (inkl. `jb_`/`JB_`/`jb-`/camelCase) + Doku; Endpoint bleibt.
- **AP-B Layout-Variante C** (Sektionen + „+") via ASCII-Mockup-Vergleich (AskUserQuestion-Previews statt Browser-Companion).
- **AP-C:** Join-Typ-Select nur am **aktiven** Pfad; Select **nicht** in den klickbaren `<a>` (aktiver Pfad = `.path-seq`-Span).
- **Insight `2026-06-28-ui-diff-review-vs-browser-smoke.md`:** Diff-Review bestätigt Code, aber CSS-Spezifität + Jinja-Template-Caching fängt nur der Browser-Smoke (+ App-Neustart bei Template-Änderung).
- **COUNT-AP:** Route blieb komplett unverändert (agg fließt verbatim durch) — Feature in nur 2 Dateien.

## Git-Status
- Branch: `master` — synchron mit `origin/master` (0/0), Working Tree **clean**
- HEAD: `c843c5d`; gh-pages remote `cf478aa`
- Version: **0.43.4**; Tests **308 passed, 2 skipped**
- Uncommitted: keine (dieser Handoff + Insight + KPI-Zeile werden mit committet) · Stash: keiner

## Wichtig für die nächste Session
- App-Start: `bash run.sh --tray`. venv = Python 3.14; Tests `./venv/bin/python -m pytest`.
- **UI-Verifikation:** Browser-Smoke via Playwright/System-`python3` (Skripte unter `.superpowers/sdd/verify_*.py` als Vorlage); bei **Template-Änderungen App neu starten** (waitress cached Jinja non-debug), JS/CSS sind live.
- Je AP/Feature: Version-Bump (`sync_version.py` + icon-rail `APP_VERSION` manuell!) + Changelog/Mirror + Roadmap/Board/Gantt (jedes Item namentlich) + Badge + Site + gh-pages-Deploy; Deutsch/NO-CDN; SDD-Final-Review nie weglassen.
- Das laufende `favicon.ico`-404 im Browser-Smoke ist **vorbestehend/benign** (kein Bug).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit LucentTools DB Explorer (SQL-Builder).
Letzte Session: 2026-06-28 16:28 (Session 11), Stand **v0.43.4, 308 Tests grün** (2 skipped), alles nach origin/master + gh-pages gepusht, github.io live.
ZUERST: git fetch + Divergenz prüfen; `./venv/bin/python -m pytest` als Baseline (venv = Python 3.14). App via `bash run.sh --tray`.
Stand: Aggregat-Kette (Tier-3/HAVING/ORDER-BY/COUNT/GROUP-BY-Fix) + UI-Umbau AP-A…AP-D fertig. Kein dringender offener Strang.
Nächste Kandidaten: **AP-E** (Zeilen Move ↑/↓ — Buttons, in `app.js` `addOrderByRow`/`addColRow`) · **AP-F** (Analyzer-Vorschläge in `core/sqlanalyze.py`) · **Cross-Schema-Joins** (große Stufe).
Merke: je Feature Version-Bump (sync_version.py + icon-rail APP_VERSION) + Roadmap/Board/Gantt + Badge + gh-pages-Deploy; Deutsch/NO-CDN; SDD-Final-Review nie weglassen; UI-Browser-Smoke prüft *gerenderte* Eigenschaften (CSS-Spezifität!) und braucht App-Neustart bei Template-Änderung (Insight 2026-06-28-ui-diff-review-vs-browser-smoke.md); Memory [[ludbxp-sqlbuilder-ui-block]].
Handoff-Datei: docs/handoffs/2026-06-28-1628.md
