# Session Handoff — 2026-06-29 20:13 (Session 15)

## Was wir gemacht haben
Sechs MINOR-Releases (v0.49.0 → v0.54.0), je eine AP, **jede** über den vollen SDD-Zyklus (Brainstorm → Spec → Plan → 4 Tasks → Task-Reviews → Whole-Branch-Review/opus → Release → master+gh-pages-Deploy). Schwerpunkt: **Subset/Migrations-Toolkit fertiggestellt** (Wave 2 abgeschlossen) + **DB-Objekt-Kategorien** (AP-63 S1/S2/S2b) als read-only Reflektions-Anreicherung.

## Erledigt
- [x] **AP-56b·Stufe 1 (v0.49.0):** Subset-Live-Zeilenzahlen — `core/subset.py::count_sql` + `core/datapreview.py::count_subset_rows` (resilient pro Tabelle) + `/api/subset/run` + UI „Zeilen zählen (live)".
- [x] **AP-56b·Stufe 2 (v0.50.0):** Subset-Daten-Dump (JSON) — `dump_subset_rows` (`cap+1`-Truncation, resilient) + `/api/subset/dump` + UI-Blob-Download.
- [x] **AP-56c (v0.51.0):** Subset-IN-Listen — `subset_keys` + `subset_in_list_sql` (Single → `IN`, Composite → portable `(a=… AND b=…) OR …`) + `/api/subset/inlists` + `.sql`-Download. **→ Wave 2 (Migration: AP-54/55/56a/56b·1+2/56c) abgeschlossen.**
- [x] **AP-63·S1 (v0.52.0):** Indizes + Check-Constraints im Tabellen-Detail (`get_indexes`/`get_check_constraints`, Model `Index`/`CheckConstraint`).
- [x] **AP-63·S2 (v0.53.0):** Trigger als neue read-only Sidebar-Kategorie — dialekt-Katalog-SQL (`_reflect_triggers`, SQLite via `sqlite_master`); schlankes Detail ohne Daten-Tab.
- [x] **AP-63·S2b (v0.54.0):** Sequences + Materialized Views als Kategorien (SQLAlchemy-nativ, PG/Oracle, skip-guarded `tests/test_pg_integration.py`).
- [x] Tests **398 passed, 3 skipped** (359→398, +39; +1 PG-Live-Test). Alles nach origin/master + gh-pages deployt (master `a57e382`, gh-pages `d325ea3`). Working Tree clean.
- [x] **Doku je AP vollständig nachgezogen** (Changelog EN/DE, Roadmap, CLAUDE.md, Kennzahlen, icon-rail/zensical, Site-Build). Insight + KPI-Zeile #15 geschrieben.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (frei wählbar):**
  - **AP-63·S3** (L) — Stored Procedures + Functions (+ Oracle Packages/Synonyms) via Pro-Dialekt-Katalog-SQL, Detail mit Quelltext. Nur live testbar (PG/Oracle/MSSQL); folgt dem in S2/S2b etablierten Kategorie-Muster.
  - **AP-63·Trigger-Fast-Follow** (S–M) — PG/Oracle-Trigger-Reflektion (eigene Katalog-Query je Dialekt); heute reflektiert `_reflect_triggers` nur SQLite (`core/loaders/sqlalchemy_loader.py`).
  - **AP-65·Stufe A** (S) — Analyzer-Fehlerstelle: sqlglots `ParseError.errors` (line/col) übernehmen (heute verworfen in `core/sqlanalyze.py`).
  - **AP-66·Stufe 1** (S–M) — Views → referenzierte Prozeduren/Funktionen aus Definitionstext extrahieren.
  - **Cleanup-AP** (S) — gesammelte projektweite Minors (s. u.).
- [ ] Bei jedem Feature: Brainstorm → Spec → Plan → SDD; je Release Version-Bump + Doku (Changelog EN/DE, Roadmap, icon-rail/zensical, Kennzahlen, Site, gh-pages).

## Entscheidungen dieser Session
- **Gebündelte AP-63·S2 nach Testbarkeit zugeschnitten:** Trigger zuerst (SQLite-CI-testbar), Sequences/Matviews als S2b — statt alles zusammen mit 2/3 ungetestet. (Insight 2026-06-29.)
- **Synthetische Naht-Tests für PG-only Features (AP-63·S2b):** Endpoint via Monkeypatch-Loader (konstruiertes Schema) + UI via Playwright `page.route`-Injektion + skip-guarded PG-Live-Test → ganze Verdrahtung CI-grün, nur der Reflektions-Schritt skip-guarded.
- **Matviews reusen das `View`-Model** (name/columns/definition) — kein neues Dataclass.
- **Model-Erweiterungen immer Trailing-Felder mit `()`-Default** (Schema/Table) → andere positionale Konstruktoren unberührt; Whole-Branch-Review verifiziert das je AP gegen jeden Konstruktor.
- **Subagenten modell-gestuft:** haiku (triviale Endpoint-Serialisierung) / sonnet (Impl + Task-Review) / opus (6× Whole-Branch-Final-Review).

## Offene Follow-ups (Backlog, nicht-blockierend)
- **Projektweit:** `web/static/js/app.js::objList` nutzt `esc` statt `escAttr` für `data-name` (gleiche Klasse wie der schon gefixte `data-table`); `find()`→undefined-Härtung via einheitlichem `findByName(kind,name)`-Helper (table/view/trigger/sequence/matview teilen das Muster).
- **Loader:** `get_indexes()` wird je Tabelle 2× gerufen (uidx + indexes) → Rohergebnis cachen.
- **AP-56c:** datetime/bytes-PK-Literale typgerecht rendern (heute best-effort; selten).
- **AP-56b:** binär-sichere Serialisierung (BLOB/bytes via `jsonify`) — tool-weit, keine Regression.
- **AP-56b·S2:** Route-Test für `incomplete=True` bei Truncation; UI-Truncation-Kommentar nach statt vor dem SELECT.
- **AP-56b·S1:** `count_sql` in skip-guarded MSSQL/Oracle-Integrationstests aufnehmen.

## Git-Status
- Branch: `master` (= origin/master `a57e382`); `gh-pages` = origin `d325ea3`
- Uncommitted: **clean** (dieser Handoff + Insight + KPI-Zeile #15 werden noch committet)
- Stash: none; alle 6 Feature-Branches gemergt (FF) + gelöscht.

## Verifikation
- `./venv/bin/python -m pytest` → **398 passed, 3 skipped** · Stand **v0.54.0**
- Browser-Smokes je AP grün (Subset-Count/Dump-Download/IN-Listen/Indizes/Trigger/Seq-Matview via `page.route`). github.io live mit v0.54.0.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit LucentTools DB Explorer.
Letzte Session: 2026-06-29 20:13 (Session 15), Stand v0.54.0, 398 Tests grün (3 skipped — MSSQL/Oracle/PG-Live), alles nach origin/master (`a57e382`) + gh-pages (`d325ea3`) gepusht, github.io live, Working Tree clean.
ZUERST: git fetch + Divergenz prüfen; `./venv/bin/python -m pytest` als Baseline (venv = Python 3.14). App via `bash run.sh --skip-setup` (Tray-GUI crasht headless).
Kontext: Reverse-Engineering-/Migrations-Tool für die HCMX-Ablösung (Oracle/MSSQL). Alt-DB verstehen → referenziell sauberer Export → neues OpenTofu-Modell.
Stand: **Wave 2 (Migration) abgeschlossen** (AP-54/55/56a/56b·1+2/56c). DB-Objekt-Kategorien: AP-63·S1 (Indizes/Checks) ✓, S2 (Trigger/SQLite) ✓, S2b (Sequences/Matviews/PG) ✓.
Nächster Schritt (frei wählbar): AP-63·S3 (Procedures/Functions, L, nur live testbar) ODER AP-63·Trigger-Fast-Follow (PG/Oracle-Trigger, S–M) ODER AP-65·Stufe A (Analyzer-Fehlerstelle, S: `core/sqlanalyze.py` verwirft sqlglots `ParseError.errors`) ODER AP-66·Stufe 1 (Views→Routinen) ODER Cleanup-AP (projektweite Minors: objList escAttr, findByName-Helper, get_indexes-Cache). Brainstorm zuerst.
Merke: je Feature Version-Bump (sync_version.py + icon-rail) + Changelog EN/DE + Roadmap/Board/Gantt + zensical + Kennzahlen-Seite (hartkodiert!) + Site + gh-pages; Deutsch/NO-CDN; SDD-Whole-Branch-Review nie weglassen; **Scheiben nach Testbarkeit schneiden** + für nur-gegen-X-Features **synthetische Daten an der Naht injizieren** (Insight 2026-06-29); Model-Erweiterungen als Trailing-Felder mit `()`-Default; UI-Smoke braucht App-Neustart bei Route/Template/Python-Änderung; Subagenten modell-gestuft (haiku/sonnet/opus).
Handoff-Datei: docs/handoffs/2026-06-29-2013.md
