# Session Handoff — 2026-06-28 22:16 (Session 13)

## Was wir gemacht haben
**AP-55 — Implied-FK-Schärfung** via Subagent-Driven Development gebaut und als **v0.47.0** ausgeliefert (Brainstorm → Spec → Plan → 4 SDD-Tasks → Final-opus-Review → Merge → gh-pages-Deploy). Danach zwei **Nutzer-Nachträge**: das Projekt-Kennzahlen-Dashboard in die Doku-Site integriert — zuerst als verlinkte Standalone-HTML, dann auf Wunsch **theme-nativ** als Markdown-Seite umgebaut. Abschließend App + Doku lokal gestartet.

## Erledigt
- [x] **AP-55 Implied-FK-Schärfung → v0.47.0** (SDD, 4 Tasks, alle Task-Reviews clean, Final-opus = *Ready to merge*, keine Critical/Important):
  - `core/implied.py`: zusätzlich **Suffix→Tabellenname**-Heuristik (Groß/Klein-, Trenner-, Plural-normalisiert) + **Confidence-Score** (hoch/mittel/niedrig); `ImpliedFK` um `confidence`/`reason` erweitert (rückwärtskompatibel). 8 neue Unit-Tests.
  - `/api/schema` liefert `implied_fks`; Info-Panel zeigt Count + Liste + Confidence-Badge (Smoke 5/5).
  - Cross-Schema-implied bewusst **Carryover** (braucht Multi-Schema-Reflection, Gate wie AP-57).
- [x] Release v0.47.0 vollständig: sync_version, icon-rail (`338`/`0.47.0`), zensical, Changelog EN+DE, Roadmap/Gantt/Board (AP-55 Offen→Erledigt), oberflaeche.md, CLAUDE.md, Site-Build.
- [x] **Merge → master** (`b6175a5`, push ✓) + **gh-pages-Deploy** (`5591cc6` ✓).
- [x] **Projekt-Kennzahlen in die Doku integriert (theme-nativ):** neue `luDBxP-docs/docs/projekt/kennzahlen.md` (Karten/Balken über `--md-*`-Variablen → Light/Dark-fähig; native MD-Tabellen + Admonition), `kz-*`-CSS in `extra.css`, Nav-Eintrag; standalone `kennzahlen.html` entfernt. Headline-Zahlen neu erhoben; COCOMO/LOC als Baseline v0.34.0 markiert.
- [x] App (:5057, reiner WSGI-Server) + Doku (:8046, `/projekt/kennzahlen/`) laufen lokal.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (nächstes AP):** **AP-56 — Entitäts-Hülle / Subset-Export** (Aufwand **L**) — schließt Wave 2 (Migration) ab. Neues `core/subset.py`: transitive FK-Closure (Eltern + Kinder rekursiv) über den bestehenden Graphen (`core/graph.py`), mit Zyklus-Schutz + Tiefenlimit; read-only Sequenz von SELECTs/Export (SQL/CSV/JSON); UI-Modus „Entität exportieren". **Brainstorm zuerst.** Konzept: `docs/concepts/2026-06-28-legacy-db-migration-tooling.md` (Abschnitt AP-56) — laut Konzept der *größte echte Migrationsnutzen*.
- [ ] Danach: **AP-57** nur falls AP-54 echte Cross-Schema-FKs nachweist (HCMX-Erwartung: nein → entfällt). Wave 3 (Tool-Tiefe): AP-63·S1/S2/S3 (Objekt-Kategorien) + AP-61 (Demo-UX), AP-62 (Passwort).

## Entscheidungen dieser Session
- **AP-55-Scope:** nur die jetzt baubare Scheibe (Single-Schema-Heuristik + Confidence + Info-Panel-Liste). Cross-Schema-implied + Runtime-Config-Muster + Graph-Kanten-Styling bewusst geschnitten (YAGNI / blockiert).
- **Confidence-Modell:** diskrete Stufen (hoch/mittel/niedrig) statt numerischer Score — ehrlicher für eine Heuristik, leicht testbar/als Badge darstellbar.
- **Kennzahlen-Integration:** theme-nativ (MD + Theme-CSS-Variablen) statt standalone-HTML-Link; statischer v0.34.0-Schnappschuss → verifizierbare Headline-Zahlen neu erhoben, Rest ehrlich als „Baseline v0.34.0" markiert. Insight `2026-06-28-kennzahlen-integration-theme-nativ-und-baseline.md`.
- **App-Start:** Tray-GUI crashte im Hintergrund-/Headless-Kontext mit DBus-Fehler → App als reiner `run.sh --skip-setup`-WSGI-Server gestartet. Tray-Menü bei Bedarf vom Desktop (`run.sh --tray`).

## Git-Status
- Branch: `master` (= origin/master `b6175a5`); `gh-pages` = origin `5591cc6`
- Uncommitted: **nach Handoff-Commit clean** (dieser Handoff + Insight + KPI-Zeile #13 werden noch committet)
- Stash: none
- Branch `ap-55-implied-fk`: gemergt + gelöscht

## Verifikation
- `./venv/bin/python -m pytest` → **338 passed, 2 skipped**
- Coverage 90 % · pyflakes 2 Info · Info-Panel-Smoke 5/5 · Kennzahlen-Seite Styling-Checks ✓
- github.io live (gh-pages `5591cc6`)

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit LucentTools DB Explorer.
Letzte Session: 2026-06-28 22:16 (Session 13), Stand v0.47.0, 338 Tests grün (2 skipped), alles nach origin/master + gh-pages gepusht, github.io live, Working Tree clean.
ZUERST: git fetch + Divergenz prüfen; `./venv/bin/python -m pytest` als Baseline (venv = Python 3.14). App via `bash run.sh --tray` (oder `--skip-setup` falls Tray-GUI zickt).
Kontext: Reverse-Engineering-/Migrations-Tool für die Ablösung von OpenText HCMX (Oracle/MSSQL). Alt-DB verstehen → referenziell sauberer Export → neues OpenTofu-Modell.
Stand: Wave 2 (Migration) — AP-54 ✓ (Cross-Schema-Diagnose) → AP-55 ✓ (Implied-FK-Schärfung, v0.47.0) → **AP-56 als nächstes**.
Nächster Schritt: **AP-56 — Entitäts-Hülle / Subset-Export** (L): neues `core/subset.py`, transitive FK-Closure (Eltern+Kinder, Zyklus-Schutz, Tiefenlimit) über `core/graph.py`, read-only SELECT-/Export-Sequenz, UI-Modus „Entität exportieren". Brainstorm zuerst. Konzept: `docs/concepts/2026-06-28-legacy-db-migration-tooling.md` (AP-56) — größter Migrationsnutzen.
Merke: je Feature Version-Bump (sync_version.py + icon-rail APP_VERSION/TEST_COUNT) + Roadmap/Board/Gantt + Changelog EN+DE + zensical + Site-Build + gh-pages; **Projekt-Kennzahlen-Seite** (`docs/projekt-kennzahlen.html` + `luDBxP-docs/docs/projekt/kennzahlen.md`) hat hartkodierte Zahlen → bei Release mitziehen; Deutsch/NO-CDN; SDD-Final-Review nie weglassen; UI-Smoke prüft gerenderte CSS-Eigenschaften + braucht App-Neustart bei Route/Template-Änderung.
Handoff-Datei: docs/handoffs/2026-06-28-2216.md
