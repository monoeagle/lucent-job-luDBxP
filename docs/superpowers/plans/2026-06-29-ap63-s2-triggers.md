# AP-63·Stufe 2 (erste Scheibe) — Trigger als Sidebar-Kategorie Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trigger als eigene read-only Sidebar-Kategorie reflektieren + anzeigen (Name, besitzende Tabelle, Quelltext) — etabliert das neue-Objekt-Kategorie-Muster, SQLite-CI-testbar.

**Architecture:** Der Loader reflektiert Trigger via dialekt-spezifischem Katalog-SQL (SQLite: `sqlite_master`; andere Dialekte → `()`); ein neues `Trigger`-Model + `Schema.triggers`-Feld tragen sie; `/api/schema` serialisiert sie; die Sidebar bekommt eine „Trigger"-Kategorie (nur bei N>0) und `openDetail` einen schlanken `trigger`-Zweig ohne „Daten"-Subtab. Demo-CMDB erhält einen Trigger.

**Tech Stack:** Python 3.10+ (venv = 3.14), Flask, SQLAlchemy (Reflection + Katalog-SQL), vanilla JS; pytest; Playwright (System-python3) für Browser-Smoke.

## Global Constraints

- **Layering:** `core/` importiert **nie** Flask. Das Model ist reine frozen Dataclasses. Der Loader darf SQLAlchemy nutzen (tut es). Web ruft Core.
- **Read-only:** nur Katalog-SELECTs + Anzeige; nichts wird ausgeführt, kein DDL erzeugt, Trigger werden nicht ausgelöst.
- **Nur Trigger, nur SQLite-Reflektion** in dieser Scheibe; andere Dialekte → `()` (dokumentiert). Sequences/Mat-Views = AP-63·S2b (separat).
- **Model-Erweiterung am Ende:** `Schema.triggers` mit Default `()` nach `views`.
- **Sidebar-Kategorie nur sichtbar bei N>0.** **Trigger-Detail ohne „Daten"-Subtab.**
- **NO CDN:** keine externen Assets. **Sprache:** UI/Doku Deutsch.
- **Version:** nie `config.APP_VERSION` von Hand — nur via `sync_version.py`.
- **Neustart-Reibung:** Route/Python-Änderungen wirken erst nach App-Neustart; JS/CSS live. Tests nutzen den Flask-Testclient.

---

### Task 1: Model + Loader — Trigger-Reflektion

**Files:**
- Modify: `core/model.py` (neue `Trigger`-Dataclass + `Schema.triggers`)
- Modify: `core/loaders/sqlalchemy_loader.py` (`_reflect_triggers` + Importe + `Schema(...)`-Aufruf)
- Create: `tests/fixtures/triggers_schema.sql`
- Modify: `tests/conftest.py` (Fixture `triggers_url`)
- Test: `tests/test_sqlalchemy_loader.py`

**Interfaces:**
- Produces:
  - `core.model.Trigger(name: str, table: str, sql: str)`
  - `Schema.triggers: tuple[Trigger,...] = ()`
  - Loader füllt `schema.triggers` (SQLite via `sqlite_master`; sonst `()`).
  - Fixture `triggers_url`.

- [ ] **Step 1: Add the test fixture schema**

Create `tests/fixtures/triggers_schema.sql`:

```sql
CREATE TABLE Account (
    id      INTEGER PRIMARY KEY,
    balance INTEGER NOT NULL
);
CREATE TABLE AuditLog (
    id     INTEGER PRIMARY KEY,
    msg    TEXT NOT NULL
);
CREATE TRIGGER trg_account_audit AFTER INSERT ON Account
BEGIN
    INSERT INTO AuditLog (msg) VALUES ('account created');
END;
```

- [ ] **Step 2: Add the conftest fixture**

In `tests/conftest.py`, after the existing `indexes_checks_url` fixture, add:

```python
@pytest.fixture
def triggers_url(tmp_path) -> str:
    """SQLite URL with one named trigger (for AP-63·S2 reflection tests)."""
    return _build_sqlite(tmp_path, "triggers.db", "triggers_schema.sql")
```

- [ ] **Step 3: Write the failing loader tests**

In `tests/test_sqlalchemy_loader.py`, append (the file already imports `SqlAlchemyLoader`):

```python
def test_loader_reflects_triggers(triggers_url):
    schema = SqlAlchemyLoader(triggers_url).load()
    by_name = {tr.name: tr for tr in schema.triggers}
    assert "trg_account_audit" in by_name
    assert by_name["trg_account_audit"].table == "Account"
    assert "CREATE TRIGGER" in by_name["trg_account_audit"].sql


def test_loader_no_triggers_is_empty(inventory_url):
    schema = SqlAlchemyLoader(inventory_url).load()
    assert schema.triggers == ()
```

- [ ] **Step 4: Run tests to verify they fail**

Run: `./venv/bin/python -m pytest tests/test_sqlalchemy_loader.py -k "triggers" -v`
Expected: FAIL — `ImportError` (no `Trigger`) or `AttributeError: 'Schema' object has no attribute 'triggers'`.

- [ ] **Step 5: Add the model dataclass + Schema field**

In `core/model.py`, add after the `View` class (before `Schema`):

```python
@dataclass(frozen=True)
class Trigger:
    name: str
    table: str       # besitzende Tabelle (tbl_name); "" falls unbekannt
    sql: str         # CREATE TRIGGER …-Quelltext
```

In the `Schema` dataclass, add a field after `views`:

```python
    triggers: tuple[Trigger, ...] = ()
```

- [ ] **Step 6: Add trigger reflection in the loader**

In `core/loaders/sqlalchemy_loader.py`:

(a) Extend the SQLAlchemy import (line 2) to add `text`:
```python
from sqlalchemy import create_engine, inspect, text
```
(b) Extend the model import (line 5) to add `Trigger`:
```python
from core.model import Column, ForeignKey, Index, CheckConstraint, Table, View, Schema, Trigger
```
(c) Add a module-level helper (e.g. after `_odbc_driver_hint` near the top of the file):
```python
def _reflect_triggers(engine) -> tuple:
    """Read-only trigger reflection. SQLite via sqlite_master; other dialects
    return () for now (SQLAlchemy has no native trigger API)."""
    if engine.dialect.name != "sqlite":
        return ()
    try:
        with engine.connect() as conn:
            rows = conn.execute(text(
                "SELECT name, tbl_name, sql FROM sqlite_master "
                "WHERE type='trigger' AND sql IS NOT NULL ORDER BY name"
            )).fetchall()
        return tuple(Trigger(r[0], r[1] or "", r[2] or "") for r in rows)
    except SQLAlchemyError:
        return ()
```
(d) Change the `Schema(...)` return (currently `return Schema(tuple(tables), tuple(views))`) to pass triggers:
```python
            return Schema(tuple(tables), tuple(views), _reflect_triggers(engine))
```

- [ ] **Step 7: Run tests to verify they pass**

Run: `./venv/bin/python -m pytest tests/test_sqlalchemy_loader.py -k "triggers" -v`
Expected: PASS (2 Tests)

- [ ] **Step 8: Run the full suite (Schema change touches constructors)**

Run: `./venv/bin/python -m pytest -q`
Expected: alle grün (390 + 2 neue = 392 passed, 2 skipped). Falls abweichend, gilt die reale Zahl.

- [ ] **Step 9: Commit**

```bash
git add core/model.py core/loaders/sqlalchemy_loader.py tests/fixtures/triggers_schema.sql tests/conftest.py tests/test_sqlalchemy_loader.py
git commit -m "feat(model): Trigger reflektieren (SQLite-Katalog) + im Schema tragen (AP-63·S2)"
```

---

### Task 2: Endpoint — `/api/schema` serialisiert Trigger

**Files:**
- Modify: `web/routes.py` (`api_schema`-Response, ~Z. 134–162)
- Test: `tests/test_api.py`

**Interfaces:**
- Consumes: `Schema.triggers` (Task 1); Fixture `triggers_url` (Task 1).
- Produces: Response-Feld `triggers: [{name, table, sql}]`.

- [ ] **Step 1: Write the failing test**

In `tests/test_api.py`, append:

```python
def test_schema_exposes_triggers(client, triggers_url):
    data = client.post("/api/schema", json={"connection_url": triggers_url}).get_json()
    by = {tr["name"]: tr for tr in data["triggers"]}
    assert "trg_account_audit" in by
    assert by["trg_account_audit"]["table"] == "Account"
    assert "CREATE TRIGGER" in by["trg_account_audit"]["sql"]


def test_schema_no_triggers_is_empty_list(client, inventory_url):
    data = client.post("/api/schema", json={"connection_url": inventory_url}).get_json()
    assert data["triggers"] == []
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `./venv/bin/python -m pytest tests/test_api.py -k "schema_exposes_triggers or no_triggers" -v`
Expected: FAIL with `KeyError: 'triggers'`

- [ ] **Step 3: Add the serialization**

In `web/routes.py`, in the `api_schema` `jsonify(...)` call, add a `triggers=` keyword (e.g. after the `views=[...]` block, before `cross_schema_fks=`):

```python
        triggers=[
            {"name": tr.name, "table": tr.table, "sql": tr.sql}
            for tr in schema.triggers
        ],
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `./venv/bin/python -m pytest tests/test_api.py -k "schema_exposes_triggers or no_triggers" -v`
Expected: PASS (2 Tests)

- [ ] **Step 5: Run the full api module**

Run: `./venv/bin/python -m pytest tests/test_api.py -q`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add web/routes.py tests/test_api.py
git commit -m "feat(subset): /api/schema liefert Trigger (AP-63·S2)"
```

---

### Task 3: Demo-DB + Sidebar-Kategorie + Detail + Smoke

**Files:**
- Modify: `sample_data/build_demo_db.py` (`_SCHEMA`: ein Trigger)
- Modify: `web/static/js/app.js` (`renderSidebar` + `openDetail`)
- Test: `tests/test_demo_db_cases.py` (Demo-Trigger-Assertion); Browser-Smoke (Playwright)

**Interfaces:**
- Consumes: `/api/schema` `triggers` (Task 2); `SCHEMA.triggers` im Client.
- Produces: „Trigger"-Sidebar-Kategorie + `trigger`-Detail-Zweig; Demo-CMDB mit `trg_vm_audit`.

- [ ] **Step 1: Add a trigger to the demo schema**

In `sample_data/build_demo_db.py`, im `_SCHEMA`-String **nach** der letzten `CREATE TABLE` (LicenseKey) und **vor** `CREATE VIEW WindowsVMs AS` einfügen:

```sql
CREATE TRIGGER trg_vm_audit AFTER INSERT ON VirtualMachine
BEGIN SELECT 1; END;
```

- [ ] **Step 2: Write the failing demo-loader test**

In `tests/test_demo_db_cases.py`, append (die Datei hat seit AP-63·S1 eine `demo_url`-Fixture + importiert `SqlAlchemyLoader`):

```python
def test_demo_has_trigger(demo_url):
    schema = SqlAlchemyLoader(demo_url).load()
    by = {tr.name: tr for tr in schema.triggers}
    assert "trg_vm_audit" in by
    assert by["trg_vm_audit"].table == "VirtualMachine"
```

- [ ] **Step 3: Run test + full demo module**

Run: `./venv/bin/python -m pytest tests/test_demo_db_cases.py -q`
Expected: PASS (bestehende Tests unberührt + neuer Test grün).

- [ ] **Step 4: Add the sidebar category**

In `web/static/js/app.js`, `renderSidebar()`, nach dem Views-Block (`<ul class="objlist">${objList(SCHEMA.views, "view")}</ul>`) und **vor** `<div class="sidebar-bottom">` einfügen:

```javascript
    ((SCHEMA.triggers && SCHEMA.triggers.length)
      ? `<h3>Trigger (${SCHEMA.triggers.length})</h3>` +
        `<ul class="objlist">${objList(SCHEMA.triggers, "trigger")}</ul>`
      : "") +
```

(Der bestehende Klick-Handler ruft für `data-kind="trigger"` bereits `openDetail("trigger", name)` — `data-action` ist nicht gesetzt.)

- [ ] **Step 5: Add the trigger branch + drop the Daten subtab for triggers**

In `web/static/js/app.js`, `openDetail(kind, name)`:

(a) Die Branch-Verzweigung um einen `trigger`-Zweig erweitern. Ersetze
```javascript
  if (kind === "table") {
```
… den `else`-(View-)Zweig lässt du stehen, aber füge VOR dem finalen `else` einen `trigger`-Zweig ein. Konkret: nach dem schließenden `}` des `if (kind === "table") { … }`-Blocks und vor `} else {` (View) einfügen:
```javascript
  } else if (kind === "trigger") {
    const tr = (SCHEMA.triggers || []).find((x) => x.name === name);
    defHtml = `<h2>Trigger: ${esc(tr.name)}</h2>` +
      `<p class="hint">auf Tabelle: ${esc(tr.table || "—")}</p>`;
    sqlText = tr.sql;
```

(b) Nach der Branch-Verzweigung (vor `panel.innerHTML = …`) ein `hasData`-Flag und die bedingte Subtab-Bar. Ersetze den `panel.innerHTML = …`-Block und das Daten-Wiring durch:
```javascript
  const hasData = kind === "table" || kind === "view";
  const dataBtn = hasData ? `<button class="subtab" data-sub="data">Daten</button>` : "";
  const dataPane = hasData ? `<div class="subpanel" data-sub="data"></div>` : "";
  panel.innerHTML =
    `<div class="detail">` +
    `<div class="subtabbar">` +
    `<button class="subtab active" data-sub="def">Definition</button>` +
    dataBtn +
    `<button class="subtab" data-sub="sql">SQL</button></div>` +
    `<div class="subpanel active" data-sub="def">${defHtml}</div>` +
    dataPane +
    `<div class="subpanel" data-sub="sql"><pre class="viewdef"></pre></div></div>`;
  panel.querySelector('.subpanel[data-sub="sql"] .viewdef').textContent =
    sqlText || "(keine Definition)";

  const dataPanel = panel.querySelector('.subpanel[data-sub="data"]');
  let dataLoaded = false;
  panel.querySelectorAll(".subtab").forEach((st) => {
    st.addEventListener("click", () => {
      panel.querySelectorAll(".subtab").forEach((x) =>
        x.classList.toggle("active", x === st));
      panel.querySelectorAll(".subpanel").forEach((p) =>
        p.classList.toggle("active", p.dataset.sub === st.dataset.sub));
      if (st.dataset.sub === "data" && !dataLoaded && dataPanel) {
        dataLoaded = true;
        loadData(name, dataPanel);
      }
    });
  });
```

- [ ] **Step 6: App neu starten + Browser-Smoke**

App neu starten (Route/Python aus Tasks 1–2 braucht Neustart):
```bash
pkill -f "run.sh|app.py|waitress" 2>/dev/null; sleep 1
bash run.sh --skip-setup &
sleep 3
```
Demo-DB neu bauen (mit Trigger):
`./venv/bin/python -c "from sample_data.build_demo_db import build; build('sample_data/demo_cmdb.db')"`

Playwright-Smoke nach `scratchpad/smoke_triggers.py` (Connect-/Sidebar-Selektoren aus einem bestehenden Smoke wie `scratchpad/smoke_subset_dump.py` übernehmen): Demo verbinden → Sidebar enthält „Trigger"-Kategorie mit `trg_vm_audit` → Trigger öffnen → Detail zeigt „auf Tabelle: VirtualMachine" und das aktive Detail enthält keinen „Daten"-Button; SQL-Subtab zeigt `CREATE TRIGGER`.

```python
from playwright.sync_api import sync_playwright
import pathlib
DB = pathlib.Path("sample_data/demo_cmdb.db").resolve()
with sync_playwright() as p:
    b = p.chromium.launch(); pg = b.new_page()
    pg.goto("http://127.0.0.1:5057")
    # ... echte Connect-/Sidebar-Schritte wie in smoke_subset_dump.py ...
    # Sidebar: Trigger-Kategorie sichtbar; Trigger-Eintrag (data-name="trg_vm_audit") klicken
    pg.wait_for_selector("h3:has-text('Trigger')")
    pg.click('li[data-kind="trigger"][data-name="trg_vm_audit"]')
    body = pg.inner_text(".panel.active")
    assert "VirtualMachine" in body
    assert "Daten" not in pg.inner_text(".panel.active .subtabbar")
    print("PASS"); b.close()
```
Run: `python3 scratchpad/smoke_triggers.py`
Expected: Ausgabe enthält `PASS`. (Selektoren an die laufende UI anpassen — der Beweis ist die Trigger-Kategorie + das trigger-Detail ohne „Daten"-Subtab.)

- [ ] **Step 7: Commit**

```bash
git add sample_data/build_demo_db.py web/static/js/app.js tests/test_demo_db_cases.py
git commit -m "feat(subset): Trigger-Sidebar-Kategorie + Trigger-Detail; Demo-CMDB erweitert (AP-63·S2)"
```

---

### Task 4: Release & Doku

**Files:**
- Modify: `config.py`/`lucent-hub.yml` (via `sync_version.py`), `luDBxP-docs/docs/javascripts/icon-rail.js`, `luDBxP-docs/zensical.toml`, `CHANGELOG.md` + `luDBxP-docs/docs/entwicklung/changelog.md`, `luDBxP-docs/docs/projekt/roadmap.md`, `luDBxP-docs/docs/projekt/kennzahlen.md`, `docs/projekt-kennzahlen.html`, `CLAUDE.md`; dann Site-Build.

**Interfaces:** keine Code-Interfaces — Release-/Doku-Schritt.

- [ ] **Step 1: Volle Suite grün**

Run: `./venv/bin/python -m pytest -q`
Expected: alle grün (390 + 5 neue = 395 passed, 2 skipped). Falls abweichend, gilt die reale Zahl — diese als TEST_COUNT verwenden.

- [ ] **Step 2: Version bump (Feature → minor)**

```bash
./venv/bin/python sync_version.py --minor   # v0.52.0 → v0.53.0
```

- [ ] **Step 3: icon-rail + zensical + Kennzahlen nachziehen**

- `luDBxP-docs/docs/javascripts/icon-rail.js`: `APP_VERSION` `0.52.0`→`0.53.0`, `TEST_COUNT` `390`→`395` (bzw. reale Zahl aus Step 1), `TEST_DATE` `2026-06-29`.
- `luDBxP-docs/zensical.toml`: `v0.52.0`→`v0.53.0`.
- `luDBxP-docs/docs/projekt/kennzahlen.md` **und** `docs/projekt-kennzahlen.html`: Version `v0.52.0`→`v0.53.0`, Tests `390`→`395` (je 2 Stellen).

- [ ] **Step 4: Changelog EN + DE-Mirror**

Eintrag `## [0.53.0] — 2026-06-29` ganz oben in beiden Changelogs: „Triggers as a new read-only sidebar category (AP-63·S2): name, owning table and source are reflected (SQLite via catalog SQL; other dialects pending) and shown in a slim trigger detail (no Data tab); demo CMDB gained a trigger. Sequences/materialized views remain AP-63·S2b." / DE-Mirror analog.

- [ ] **Step 5: Roadmap — AP-63·S2 aufsplitten**

In `luDBxP-docs/docs/projekt/roadmap.md`: den `AP-63 · Stufe 2`-Eintrag im Offen-Abschnitt auf den verbleibenden Teil reduzieren (**AP-63·S2b** — Sequences + Materialized Views, PG-only; PG/Oracle-Trigger als Fast-Follow); im Erledigt-Abschnitt eine `**v0.53.0** (2026-06-29):`-Gruppe mit AP-63·S2 (Trigger-Kategorie) **vor** der `v0.52.0`-Gruppe einfügen. Gerenderte Übersicht nach Build gegenprüfen (jedes Item namentlich).

- [ ] **Step 6: CLAUDE.md „Bekannte Einschränkungen"**

Tier-Hinweis ergänzen: „Trigger-Kategorie (AP-63·S2, v0.53.0): Trigger werden read-only als eigene Sidebar-Kategorie reflektiert (SQLite via `sqlite_master`; andere Dialekte noch nicht) — Name/Tabelle/Quelltext, schlankes Detail ohne Daten-Tab. Sequences/Materialized Views = AP-63·S2b (PG-only) offen."

- [ ] **Step 7: Site-Build**

```bash
cd luDBxP-docs && ./run_luDBxP_docs.sh --build
```
Danach prüfen: keine alte `0.52.0` außerhalb von Changelog/Roadmap/Handoffs; Site zeigt `0.53.0` + neue Testzahl.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "docs: Release v0.53.0 — AP-63·S2 (Trigger-Sidebar-Kategorie)"
```

(Merge nach master, Push, gh-pages-Deploy erfolgen nach dem finalen Whole-Branch-Review durch den Controller.)

---

## Self-Review (vom Plan-Autor durchgeführt)

- **Spec-Abdeckung:** §1 Reflection (`_reflect_triggers`) → Task 1; §2 Model → Task 1; §3 Endpoint → Task 2; §4 Sidebar → Task 3; §5 Detail (trigger-Zweig, kein Daten-Subtab) → Task 3; §6 Demo+Tests → Task 1 (Fixture-Loader) + Task 2 (Endpoint) + Task 3 (Demo + Smoke); §7 Scope-Cuts → respektiert; §8 Release → Task 4. Keine Lücke.
- **Platzhalter:** keine TBD — alle Edits mit konkretem Code/Befehl (inkl. der zwei nötigen Import-Ergänzungen `text` + `Trigger`).
- **Typkonsistenz:** `Trigger(name, table, sql)`, `Schema.triggers`, `_reflect_triggers(engine)->tuple` identisch in Task 1→2→3; `/api/schema`-Feld `triggers:[{name,table,sql}]` durchgängig; UI liest `SCHEMA.triggers`/`tr.table`/`tr.sql` exakt so; `hasData` steuert den fehlenden Daten-Subtab für Trigger.
- **Testzahlen:** Erwartungswerte 392 (nach Task 1) / 395 (final) ab Baseline 390; bei Abweichung gilt die reale Suite-Ausgabe (Release-Task nennt das ausdrücklich).
